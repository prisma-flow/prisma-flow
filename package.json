{
  "name": "prisma-flow-root",
  "version": "0.3.2",
  "private": true,
  "license": "MIT",
  "packageManager": "npm@11.8.0",
  "engines": {
    "node": ">=20"
  },
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "turbo run build",
    "build:cli": "turbo run build --filter=prisma-flow",
    "build:dashboard": "turbo run build --filter=dashboard",
    "dev": "turbo run dev --parallel",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "format": "biome check --write .",
    "format:check": "biome check .",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules",
    "security:audit": "npm audit --omit=dev --workspaces --audit-level=high",
    "test:packaging": "node scripts/test-packaging.mjs",
    "verify:release": "npm run format:check && npm run lint && npm run typecheck && npm test && npm run build && npm run test:packaging && npm run security:audit",
    "prepare": "husky || true"
  },
  "overrides": {
    "postcss": "^8.5.15",
    "next": {
      "postcss": "^8.5.15"
    }
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "turbo": "^2.10.7",
    "prettier": "^3.9.6",
    "husky": "^9.1.0",
    "lint-staged": "^15.0.0"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": ["biome check --write"],
    "*.{json,md,yaml,yml}": ["prettier --write"]
  }
}
